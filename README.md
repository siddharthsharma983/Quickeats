# Quickeats
QuickEats — A food ordering app (MVP). Full-stack: React (Frontend) + Node.js/Express (Backend) + MongoDB Atlas. Includes restaurant listing, menu, cart, checkout &amp; real-time order tracking.
